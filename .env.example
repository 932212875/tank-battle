# Local default is ws://localhost:8080 when this is unset.
# For Netlify production, set this in Netlify environment variables to your hosted WebSocket backend.
VITE_WS_URL=wss://your-tank-battle-server.example.com
